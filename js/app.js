function toggleMenu(){document.getElementById("side")?.classList.toggle("open")}function openModal(){document.getElementById("modal")?.classList.add("show")}function closeModal(){document.getElementById("modal")?.classList.remove("show")}function deleteRow(btn){if(confirm("Delete this record?"))btn.closest("tr").remove()}function editRow(){alert("Edit this record after Oracle database integration is connected.")}function searchTable(){const input=document.getElementById("search");const q=input?input.value.toLowerCase():"";document.querySelectorAll("#dataRows tr").forEach(r=>r.style.display=r.innerText.toLowerCase().includes(q)?"":"none")}function saveRecord(){alert("Record saved in the interface for now. Connect this form to Oracle for real database storage.");closeModal()}document.addEventListener("click",e=>{if(e.target.id==="modal")closeModal()});